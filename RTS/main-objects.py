import pygame
pygame.init()

# TITLE
TITLE = "RTS"

# Screen size
S_WIDTH = 1280
S_HEIGHT = 720

# RGB colors
textcolor = (255, 255, 255)
fillcolor = (0, 0, 0)

# clock to update game events and frames
clock = pygame.time.Clock()